{
  "name": "medical-appointment",
  "version": "1.0.0",
  "description": "It's an application for managing medical appointments based on `Node.js`, `PostgreSQL`, etc.",
  "main": "index.js",
  "scripts": {
    "start": "node ./src/index.js",
    "dev": "nodemon ./src/index.js -e js --watch",
    "test": "jest --verbose --watch",
    "test-coverage": "jest --verbose --coverage=true",
    "test-ci": "jest --verbose --ci --coverage=true",
    "sequelize-sync": "node ./src/db/sequelize/sync.js"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.1.0",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "express": "^4.17.1",
    "pg": "^8.3.0",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.3.3",
    "snake-case": "^3.0.3"
  }
}
